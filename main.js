function scrollTop() {
    window.scrollTo({"top": 0, "behavior": "smooth"});
}

function reloadPage() {
    location.reload();
}

function locationMove(name) {
    location.href = name;
}

function openSite(name) {
    window.open(name, '_blank');
}